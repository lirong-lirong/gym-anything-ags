{
  "id": "voip_phone_audio_package@1",
  "version": "1.0",
  "env_id": "vlc_env@0.1",
  "description": "You are a small-business IT consultant deploying a new Asterisk-based VoIP phone system for a dental clinic. Process the raw 44.1 kHz stereo recordings located in /home/ga/Music/raw_recordings/ to meet the technical requirements specified in /home/ga/Documents/phone_system_spec.txt (8 kHz, mono, 16-bit PCM WAV).\n\nCreate the following deliverables in /home/ga/Music/phone_system/:\n\n1. 'main_greeting.wav': The greeting_main.wav file converted to 8 kHz mono 16-bit PCM WAV.\n2. 'ivr_full_menu.wav': A concatenation of the greeting followed by the sales, support, and billing menu options in order, formatted as 8 kHz mono 16-bit PCM WAV.\n3. 'hold_music.wav': The source hold music looped and trimmed to exactly 60.0 seconds, formatted as 8 kHz mono 16-bit PCM WAV.\n4. 'voicemail.wav': The voicemail prompt with a 1000 Hz beep tone (0.5 seconds duration) and exactly 3 seconds of silence appended to the end, formatted as 8 kHz mono 16-bit PCM WAV.\n5. 'ivr_full_menu.mp3': A web-backup version of the full IVR menu encoded as a 64 kbps mono MP3.\n6. 'manifest.json': A JSON file documenting the five output files with keys: 'filename', 'purpose', 'duration_seconds', 'sample_rate', 'channels', and 'format'.",
  "difficulty": "hard",
  "init": {
    "timeout_sec": 900,
    "max_steps": 60,
    "reward_type": "sparse"
  },
  "hooks": {
    "pre_task": "/workspace/tasks/voip_phone_audio_package/setup_task.sh",
    "post_task": "/workspace/tasks/voip_phone_audio_package/export_result.sh"
  },
  "metadata": {
    "expected_outputs": [
      "main_greeting.wav",
      "ivr_full_menu.wav",
      "hold_music.wav",
      "voicemail.wav",
      "ivr_full_menu.mp3",
      "manifest.json"
    ],
    "target_sample_rate": 8000,
    "target_channels": 1,
    "target_codec": "pcm_s16le",
    "hold_music_duration": 60.0
  },
  "success": {
    "mode": "vlm_checklist",
    "spec": {
      "checklist": "vlm_checklist.json"
    }
  }
}
