{
  "id": "extract_expenses_to_csv@1",
  "version": "1.0",
  "env_id": "thunderbird_env@0.1",
  "description": "Extract the Name, Department, and Total reimbursement amount from the five expense report emails in the Thunderbird Inbox. Compile this information into a CSV file at ~/Documents/expense_summary.csv using the headers Name,Department,Total. Ensure the Total values are recorded as numeric values without currency symbols.",
  "difficulty": "medium",
  "init": {
    "timeout_sec": 300,
    "max_steps": 50,
    "reward_type": "sparse"
  },
  "hooks": {
    "pre_task": "/workspace/tasks/extract_expenses_to_csv/setup_task.sh",
    "post_task": "/workspace/tasks/extract_expenses_to_csv/export_result.sh"
  },
  "metadata": {
    "expected_output_path": "/home/ga/Documents/expense_summary.csv",
    "expected_headers": [
      "Name",
      "Department",
      "Total"
    ],
    "expected_rows_count": 5
  },
  "success": {
    "mode": "vlm_checklist",
    "spec": {
      "checklist": "vlm_checklist.json"
    }
  }
}
