{
  "id": "configure_disk_space_management@1",
  "version": "1.0",
  "env_id": "thunderbird_env@0.1",
  "description": "Configure Thunderbird to aggressively manage disk space by disabling the Global Search and Indexer and setting the folder compaction threshold to 15 MB. For Local Folders, enable the option to empty trash on exit and update the retention policy to delete messages older than 60 days. Manually empty the Trash folder to complete the cleanup.",
  "difficulty": "medium",
  "init": {
    "timeout_sec": 240,
    "max_steps": 40,
    "reward_type": "sparse"
  },
  "hooks": {
    "pre_task": "/workspace/tasks/configure_disk_space_management/setup_task.sh",
    "post_task": "/workspace/tasks/configure_disk_space_management/export_result.sh"
  },
  "metadata": {
    "expected_indexer_enabled": "false",
    "expected_purge_threshold": "15",
    "expected_empty_trash": "true",
    "expected_retain_by": "2",
    "expected_days_to_keep": "60",
    "expected_trash_count": 0
  },
  "success": {
    "mode": "vlm_checklist",
    "spec": {
      "checklist": "vlm_checklist.json"
    }
  }
}
