{
  "id": "fix_custom_sast_scanner@1",
  "version": "1.0",
  "env_id": "vscode_env@0.1",
  "description": "You are an Information Security Engineer maintaining a custom Python SAST tool used in your CI/CD pipeline. Following a Python 3.10 upgrade, the AST-based scanner at /home/ga/workspace/sast_scanner/ is failing: it crashes on match/case syntax, misses vulnerabilities due to import aliases and f-strings, and flags safe code regarding variable scope leakage and safe secret referencing. Fix the five critical bugs in visitor.py so that all tests in tests/test_visitor.py pass without breaking existing detection logic.",
  "difficulty": "hard",
  "init": {
    "timeout_sec": 1800,
    "max_steps": 120,
    "reward_type": "sparse"
  },
  "hooks": {
    "pre_task": "/workspace/tasks/fix_custom_sast_scanner/setup_task.sh",
    "post_task": "/workspace/tasks/fix_custom_sast_scanner/export_result.sh"
  },
  "metadata": {
    "occupation": "Information Security Engineer",
    "industry": "Computer and Mathematical",
    "pass_threshold": 60,
    "vlm_criteria": "The agent must open VS Code, view and edit visitor.py, run pytest, and iteratively fix the AST parsing bugs."
  },
  "success": {
    "mode": "vlm_checklist",
    "spec": {
      "checklist": "vlm_checklist.json"
    }
  }
}
